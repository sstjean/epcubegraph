# vmagent scrape configuration for local development
# Scrapes mock-exporter instead of real epcube-exporter

scrape_configs:
  - job_name: epcube
    static_configs:
      - targets: ["mock-exporter:9191"]
    metrics_path: /metrics
    scrape_interval: 60s
    scrape_timeout: 30s
