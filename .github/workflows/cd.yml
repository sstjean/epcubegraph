name: CD

on:
  # Deploy after CI passes on any branch
  workflow_run:
    workflows: [CI]
    types: [completed]

  # Deploy staging as a PR check
  pull_request:
    branches: [main]

  # Manual trigger for any environment
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      destroy:
        description: 'Destroy resources after validation (for ephemeral test deployments)'
        required: false
        default: false
        type: boolean

# Limit to one deployment at a time per environment
concurrency:
  group: deploy-${{ github.event.inputs.environment || (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' && 'production' || 'staging') }}
  cancel-in-progress: false

env:
  TF_VAR_location: 'eastus'
  ARM_USE_OIDC: 'true'
  TFSTATE_STORAGE_ACCOUNT: 'tfstateepcubegraph'
  TFSTATE_RESOURCE_GROUP: 'tfstate-rg'

permissions:
  id-token: write   # OIDC token for Azure login
  contents: read

jobs:
  # ── Staging: deploy on every push (via CI) and on PRs to main ──
  check-ci:
    runs-on: ubuntu-latest
    # Skip production path (merge to main via workflow_run) — that's handled below
    if: >
      github.event_name == 'pull_request' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging') ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch != 'main')
    steps:
      - run: echo "Staging deployment — CI passed, PR, or manual dispatch"

  deploy:
    needs: check-ci
    runs-on: ubuntu-latest
    environment: staging

    env:
      TF_VAR_environment_name: 'epcubegraph-staging'

    outputs:
      resource_group: ${{ steps.tf-output.outputs.resource_group }}
      api_fqdn: ${{ steps.tf-output.outputs.api_fqdn }}
      vm_fqdn: ${{ steps.tf-output.outputs.vm_fqdn }}
      exporter_fqdn: ${{ steps.tf-output.outputs.exporter_fqdn }}

    steps:
      - uses: actions/checkout@v6

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Zero-trust: detect runner IP and whitelist on tfstate storage
      - name: Detect runner IP
        id: ip
        run: |
          RUNNER_IP=$(curl -sf https://api.ipify.org)
          echo "runner_ip=${RUNNER_IP}" >> "$GITHUB_OUTPUT"
          echo "::add-mask::${RUNNER_IP}"
          echo "✓ Detected runner IP"

      - name: Whitelist runner IP on tfstate storage
        run: |
          az storage account network-rule add \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true

      # Whitelist runner IP on Key Vault so Terraform can read existing secrets
      - name: Whitelist runner IP on Key Vault
        run: |
          KV_NAME="epcubegraph-staging-kv"
          if az keyvault show --name "$KV_NAME" --query name -o tsv 2>/dev/null; then
            az keyvault network-rule add \
              --name "$KV_NAME" \
              --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
              --output none 2>/dev/null || true
            echo "✓ Runner IP added to Key Vault firewall"
          else
            echo "Key Vault does not exist yet — skipping (first deploy)"
          fi

      # Wait for firewall rules to propagate (probe tfstate storage directly)
      - name: Wait for firewall propagation
        run: |
          echo "Waiting for tfstate storage firewall propagation..."
          for attempt in $(seq 1 30); do
            if az storage container list \
              --account-name "$TFSTATE_STORAGE_ACCOUNT" \
              --auth-mode login \
              --output none 2>/dev/null; then
              echo "✓ tfstate storage accessible (attempt $attempt)"
              exit 0
            fi
            echo "Attempt $attempt/30 — not yet accessible, waiting 15s..."
            sleep 15
          done
          echo "::error::tfstate storage not accessible after 30 attempts (7.5 min)"
          exit 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Terraform Init
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: terraform init -input=false -backend-config="key=epcubegraph-staging.tfstate"

      # Phase 1: Deploy infrastructure (without container images)
      # Runner IP is passed so Terraform whitelists it on Key Vault and app storage
      - name: Terraform Apply — Infrastructure
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_epcube_username: ${{ secrets.EPCUBE_USERNAME }}
          TF_VAR_epcube_password: ${{ secrets.EPCUBE_PASSWORD }}
          TF_VAR_allowed_ips: '["${{ steps.ip.outputs.runner_ip }}"]'
        run: terraform apply -auto-approve -input=false

      # Phase 2: Build and push container images
      # Re-authenticate — OIDC token may have expired during long infrastructure apply
      - name: Azure Re-Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get ACR details
        id: acr
        working-directory: infra
        run: |
          echo "name=$(terraform output -raw acr_name)" >> "$GITHUB_OUTPUT"
          echo "server=$(terraform output -raw acr_login_server)" >> "$GITHUB_OUTPUT"

      - name: ACR Login
        run: az acr login --name ${{ steps.acr.outputs.name }}

      - name: Build & Push API image
        id: api-image
        run: |
          IMAGE="${{ steps.acr.outputs.server }}/epcubegraph-api:${{ github.sha }}"
          docker build --tag "$IMAGE" --file api/Dockerfile api/
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Build & Push exporter image
        id: exporter-image
        run: |
          IMAGE="${{ steps.acr.outputs.server }}/epcube-exporter:${{ github.sha }}"
          docker build --tag "$IMAGE" --file local/epcube-exporter/Dockerfile local/epcube-exporter/
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      # Phase 3: Deploy container apps with built images
      # Re-authenticate — OIDC token may have expired during Docker builds
      - name: Azure Re-Login for Phase 3 (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Apply — Container Apps
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_epcube_username: ${{ secrets.EPCUBE_USERNAME }}
          TF_VAR_epcube_password: ${{ secrets.EPCUBE_PASSWORD }}
          TF_VAR_api_image: ${{ steps.api-image.outputs.image }}
          TF_VAR_epcube_image: ${{ steps.exporter-image.outputs.image }}
          TF_VAR_allowed_ips: '["${{ steps.ip.outputs.runner_ip }}"]'
        run: terraform apply -auto-approve -input=false

      - name: Capture Terraform outputs
        id: tf-output
        working-directory: infra
        run: |
          echo "resource_group=$(terraform output -raw resource_group_name)" >> "$GITHUB_OUTPUT"
          echo "api_fqdn=$(terraform output -raw api_fqdn)" >> "$GITHUB_OUTPUT"
          echo "vm_fqdn=$(terraform output -raw vm_fqdn)" >> "$GITHUB_OUTPUT"
          echo "exporter_fqdn=$(terraform output -raw exporter_fqdn)" >> "$GITHUB_OUTPUT"

      # Zero-trust cleanup: remove runner IP from tfstate storage and Key Vault (always runs)
      - name: Remove runner IP from firewalls
        if: always() && steps.ip.outputs.runner_ip
        run: |
          az storage account network-rule remove \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from tfstate firewall"
          KV_NAME="epcubegraph-staging-kv"
          az keyvault network-rule remove \
            --name "$KV_NAME" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from Key Vault firewall"

  validate:
    needs: deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Wait for container apps to stabilize after deployment
      - name: Wait for deployment to stabilize
        run: sleep 60

      - name: Run deployment validation
        run: ./infra/validate-deployment.sh --rg ${{ needs.deploy.outputs.resource_group }}

      - name: Smoke test — API health endpoint
        run: |
          API_FQDN="${{ needs.deploy.outputs.api_fqdn }}"
          if [ -n "$API_FQDN" ] && [ "$API_FQDN" != "" ]; then
            for i in 1 2 3 4 5; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${API_FQDN}/api/v1/health" 2>/dev/null) || true
              if [ "$STATUS" = "200" ] || [ "$STATUS" = "503" ]; then
                echo "✓ API health endpoint responded: HTTP $STATUS"
                break
              fi
              echo "Attempt $i: HTTP $STATUS — retrying in 15s..."
              sleep 15
            done
            if [ "$STATUS" != "200" ] && [ "$STATUS" != "503" ]; then
              echo "::error::API health endpoint did not respond after 5 attempts"
              exit 1
            fi
          else
            echo "API not deployed (no FQDN) — skipping smoke test"
          fi

      - name: Smoke test — VictoriaMetrics (internal-only check)
        run: |
          echo "✓ VictoriaMetrics is internal-only (zero-trust) — no external smoke test needed"
          echo "  API health endpoint validates internal connectivity"

      - name: Smoke test — epcube-exporter health
        run: |
          EXPORTER_FQDN="${{ needs.deploy.outputs.exporter_fqdn }}"
          if [ -n "$EXPORTER_FQDN" ] && [ "$EXPORTER_FQDN" != "" ]; then
            for i in 1 2 3 4 5; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${EXPORTER_FQDN}/health" 2>/dev/null) || true
              if [ "$STATUS" = "200" ] || [ "$STATUS" = "503" ]; then
                echo "✓ epcube-exporter health responded: HTTP $STATUS"
                break
              fi
              echo "Attempt $i: HTTP $STATUS — retrying in 15s..."
              sleep 15
            done
            if [ "$STATUS" != "200" ] && [ "$STATUS" != "503" ]; then
              echo "::error::epcube-exporter did not respond after 3 attempts"
              exit 1
            fi
          else
            echo "epcube-exporter not deployed (no FQDN) — skipping smoke test"
          fi

  # ── Production: deploy only on merge to main ──
  check-ci-prod:
    runs-on: ubuntu-latest
    if: >
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production')
    steps:
      - run: echo "Production deployment — merge to main or manual dispatch"

  deploy-prod:
    needs: check-ci-prod
    runs-on: ubuntu-latest
    environment: production

    env:
      TF_VAR_environment_name: 'epcubegraph'

    outputs:
      resource_group: ${{ steps.tf-output.outputs.resource_group }}
      api_fqdn: ${{ steps.tf-output.outputs.api_fqdn }}
      vm_fqdn: ${{ steps.tf-output.outputs.vm_fqdn }}
      exporter_fqdn: ${{ steps.tf-output.outputs.exporter_fqdn }}

    steps:
      - uses: actions/checkout@v6

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Zero-trust: detect runner IP and whitelist on tfstate storage
      - name: Detect runner IP
        id: ip
        run: |
          RUNNER_IP=$(curl -sf https://api.ipify.org)
          echo "runner_ip=${RUNNER_IP}" >> "$GITHUB_OUTPUT"
          echo "::add-mask::${RUNNER_IP}"
          echo "✓ Detected runner IP"

      - name: Whitelist runner IP on tfstate storage
        run: |
          az storage account network-rule add \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true

      # Whitelist runner IP on Key Vault so Terraform can read existing secrets
      - name: Whitelist runner IP on Key Vault
        run: |
          KV_NAME="epcubegraph-kv"
          if az keyvault show --name "$KV_NAME" --query name -o tsv 2>/dev/null; then
            az keyvault network-rule add \
              --name "$KV_NAME" \
              --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
              --output none 2>/dev/null || true
            echo "✓ Runner IP added to Key Vault firewall"
          else
            echo "Key Vault does not exist yet — skipping (first deploy)"
          fi

      # Wait for firewall rules to propagate (probe tfstate storage directly)
      - name: Wait for firewall propagation
        run: |
          echo "Waiting for tfstate storage firewall propagation..."
          for attempt in $(seq 1 30); do
            if az storage container list \
              --account-name "$TFSTATE_STORAGE_ACCOUNT" \
              --auth-mode login \
              --output none 2>/dev/null; then
              echo "✓ tfstate storage accessible (attempt $attempt)"
              exit 0
            fi
            echo "Attempt $attempt/30 — not yet accessible, waiting 15s..."
            sleep 15
          done
          echo "::error::tfstate storage not accessible after 30 attempts (7.5 min)"
          exit 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Terraform Init
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: terraform init -input=false -backend-config="key=epcubegraph-production.tfstate"

      - name: Terraform Apply — Infrastructure
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_epcube_username: ${{ secrets.EPCUBE_USERNAME }}
          TF_VAR_epcube_password: ${{ secrets.EPCUBE_PASSWORD }}
          TF_VAR_allowed_ips: '["${{ steps.ip.outputs.runner_ip }}"]'
        run: terraform apply -auto-approve -input=false

      # Re-authenticate — OIDC token may have expired during long infrastructure apply
      - name: Azure Re-Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get ACR details
        id: acr
        working-directory: infra
        run: |
          echo "name=$(terraform output -raw acr_name)" >> "$GITHUB_OUTPUT"
          echo "server=$(terraform output -raw acr_login_server)" >> "$GITHUB_OUTPUT"

      - name: ACR Login
        run: az acr login --name ${{ steps.acr.outputs.name }}

      - name: Build & Push API image
        id: api-image
        run: |
          IMAGE="${{ steps.acr.outputs.server }}/epcubegraph-api:${{ github.sha }}"
          docker build --tag "$IMAGE" --file api/Dockerfile api/
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Build & Push exporter image
        id: exporter-image
        run: |
          IMAGE="${{ steps.acr.outputs.server }}/epcube-exporter:${{ github.sha }}"
          docker build --tag "$IMAGE" --file local/epcube-exporter/Dockerfile local/epcube-exporter/
          docker push "$IMAGE"
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      # Re-authenticate — OIDC token may have expired during Docker builds
      - name: Azure Re-Login for Container Apps (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Apply — Container Apps
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_epcube_username: ${{ secrets.EPCUBE_USERNAME }}
          TF_VAR_epcube_password: ${{ secrets.EPCUBE_PASSWORD }}
          TF_VAR_api_image: ${{ steps.api-image.outputs.image }}
          TF_VAR_epcube_image: ${{ steps.exporter-image.outputs.image }}
          TF_VAR_allowed_ips: '["${{ steps.ip.outputs.runner_ip }}"]'
        run: terraform apply -auto-approve -input=false

      - name: Capture Terraform outputs
        id: tf-output
        working-directory: infra
        run: |
          echo "resource_group=$(terraform output -raw resource_group_name)" >> "$GITHUB_OUTPUT"
          echo "api_fqdn=$(terraform output -raw api_fqdn)" >> "$GITHUB_OUTPUT"
          echo "vm_fqdn=$(terraform output -raw vm_fqdn)" >> "$GITHUB_OUTPUT"
          echo "exporter_fqdn=$(terraform output -raw exporter_fqdn)" >> "$GITHUB_OUTPUT"

      # Zero-trust cleanup: remove runner IP from tfstate storage and Key Vault (always runs)
      - name: Remove runner IP from firewalls
        if: always() && steps.ip.outputs.runner_ip
        run: |
          az storage account network-rule remove \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from tfstate firewall"
          KV_NAME="epcubegraph-kv"
          az keyvault network-rule remove \
            --name "$KV_NAME" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from Key Vault firewall"

  validate-prod:
    needs: deploy-prod
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Wait for deployment to stabilize
        run: sleep 60

      - name: Run deployment validation
        run: ./infra/validate-deployment.sh --rg ${{ needs.deploy-prod.outputs.resource_group }}

      - name: Smoke test — API health endpoint
        run: |
          API_FQDN="${{ needs.deploy-prod.outputs.api_fqdn }}"
          if [ -n "$API_FQDN" ] && [ "$API_FQDN" != "" ]; then
            for i in 1 2 3 4 5; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${API_FQDN}/api/v1/health" 2>/dev/null) || true
              if [ "$STATUS" = "200" ] || [ "$STATUS" = "503" ]; then
                echo "✓ API health endpoint responded: HTTP $STATUS"
                break
              fi
              echo "Attempt $i: HTTP $STATUS — retrying in 15s..."
              sleep 15
            done
            if [ "$STATUS" != "200" ] && [ "$STATUS" != "503" ]; then
              echo "::error::API health endpoint did not respond after 5 attempts"
              exit 1
            fi
          else
            echo "API not deployed (no FQDN) — skipping smoke test"
          fi

      - name: Smoke test — VictoriaMetrics (internal-only check)
        run: |
          echo "✓ VictoriaMetrics is internal-only (zero-trust) — no external smoke test needed"
          echo "  API health endpoint validates internal connectivity"

      - name: Smoke test — epcube-exporter health
        run: |
          EXPORTER_FQDN="${{ needs.deploy-prod.outputs.exporter_fqdn }}"
          if [ -n "$EXPORTER_FQDN" ] && [ "$EXPORTER_FQDN" != "" ]; then
            for i in 1 2 3 4 5; do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 15 "https://${EXPORTER_FQDN}/health" 2>/dev/null) || true
              if [ "$STATUS" = "200" ] || [ "$STATUS" = "503" ]; then
                echo "✓ epcube-exporter health responded: HTTP $STATUS"
                break
              fi
              echo "Attempt $i: HTTP $STATUS — retrying in 15s..."
              sleep 15
            done
            if [ "$STATUS" != "200" ] && [ "$STATUS" != "503" ]; then
              echo "::error::epcube-exporter did not respond after 5 attempts"
              exit 1
            fi
          else
            echo "epcube-exporter not deployed (no FQDN) — skipping smoke test"
          fi

  # Tear down: only on manual request (no auto-destroy)
  destroy:
    needs: [deploy, validate]
    runs-on: ubuntu-latest
    if: >
      always() &&
      needs.validate.result == 'success' &&
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.destroy == 'true'

    env:
      TF_VAR_environment_name: ${{ github.event.inputs.environment == 'production' && 'epcubegraph' || 'epcubegraph-staging' }}

    steps:
      - uses: actions/checkout@v6

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Zero-trust: detect runner IP and whitelist on tfstate storage
      - name: Detect runner IP
        id: ip
        run: |
          RUNNER_IP=$(curl -sf https://api.ipify.org)
          echo "runner_ip=${RUNNER_IP}" >> "$GITHUB_OUTPUT"
          echo "::add-mask::${RUNNER_IP}"
          echo "✓ Detected runner IP"

      - name: Whitelist runner IP on tfstate storage
        run: |
          az storage account network-rule add \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true

      # Whitelist runner IP on Key Vault so Terraform can read existing secrets
      - name: Whitelist runner IP on Key Vault
        run: |
          ENV="${{ github.event.inputs.environment || 'staging' }}"
          if [ "$ENV" = "production" ]; then
            KV_NAME="epcubegraph-kv"
          else
            KV_NAME="epcubegraph-${ENV}-kv"
          fi
          if az keyvault show --name "$KV_NAME" --query name -o tsv 2>/dev/null; then
            az keyvault network-rule add \
              --name "$KV_NAME" \
              --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
              --output none 2>/dev/null || true
            echo "✓ Runner IP added to Key Vault firewall"
          else
            echo "Key Vault does not exist yet — skipping"
          fi

      # Wait for firewall rules to propagate (probe tfstate storage directly)
      - name: Wait for firewall propagation
        run: |
          echo "Waiting for tfstate storage firewall propagation..."
          for attempt in $(seq 1 30); do
            if az storage container list \
              --account-name "$TFSTATE_STORAGE_ACCOUNT" \
              --auth-mode login \
              --output none 2>/dev/null; then
              echo "✓ tfstate storage accessible (attempt $attempt)"
              exit 0
            fi
            echo "Attempt $attempt/30 — not yet accessible, waiting 15s..."
            sleep 15
          done
          echo "::error::tfstate storage not accessible after 30 attempts (7.5 min)"
          exit 1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          ENV="${{ github.event.inputs.environment || 'staging' }}"
          terraform init -input=false -backend-config="key=epcubegraph-${ENV}.tfstate"

      # Re-authenticate — OIDC token may have expired during firewall propagation wait
      - name: Azure Re-Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Destroy
        working-directory: infra
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_epcube_username: ${{ secrets.EPCUBE_USERNAME }}
          TF_VAR_epcube_password: ${{ secrets.EPCUBE_PASSWORD }}
          TF_VAR_allowed_ips: '["${{ steps.ip.outputs.runner_ip }}"]'
        run: |
          # First attempt may fail due to provider ordering (e.g., Entra app
          # deleted before its redirect URIs resource). Refresh + retry resolves
          # stale references in state.
          if ! terraform destroy -auto-approve -input=false; then
            echo "⚠ First destroy attempt failed — refreshing state and retrying..."
            terraform refresh -input=false
            terraform destroy -auto-approve -input=false
          fi

      # Zero-trust cleanup: remove runner IP from tfstate storage and Key Vault (always runs)
      - name: Remove runner IP from firewalls
        if: always() && steps.ip.outputs.runner_ip
        run: |
          az storage account network-rule remove \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from tfstate firewall"
          ENV="${{ github.event.inputs.environment || 'staging' }}"
          if [ "$ENV" = "production" ]; then
            KV_NAME="epcubegraph-kv"
          else
            KV_NAME="epcubegraph-${ENV}-kv"
          fi
          az keyvault network-rule remove \
            --name "$KV_NAME" \
            --ip-address "${{ steps.ip.outputs.runner_ip }}/32" \
            --output none 2>/dev/null || true
          echo "✓ Runner IP removed from Key Vault firewall"
